<div class="max-w-sm bg-white border border-gray-200 rounded-lg shadow-sm dark:bg-gray-800 dark:border-gray-700">
  {% block media %}
    <a href="#">
      <img class="rounded-t-lg" src="https://fastly.picsum.photos/id/844/500/300.jpg?hmac=YYfLpvfj0qXAkYxWHoYOAxBejK0sZyxMiRZuUEeOliY" alt="This should be replaced" />
    </a>
  {% endblock %}
  <div class="p-5">
    <a href="#">
      <h5 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white">
        {#
          Some "simple" strings could be passed as properties... other people prefer to pass then as slots. This is
          also a decision that you need to make. #}
        {{ title }}
      </h5>
    </a>
    <div class="mb-3 font-normal text-gray-700 dark:text-gray-400">
      {#
        This could be a complex strig with formatting and additional markup... or not... so up to you to make it a
        property or slot.
      #}
      {% block content %}
        The content will render here
      {% endblock %}
    </div>
    <a href="#" class="inline-flex items-center px-3 py-2 text-sm font-medium text-center text-white bg-blue-700 rounded-lg hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">
      {# A decision that should be considered, do we pass the labels with translations or should we use the `t` filter? #}
      {{ read_more_label }}
      <svg class="rtl:rotate-180 w-3.5 h-3.5 ms-2" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 10">
        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 5h12m0 0L9 1m4 4L9 9"/>
      </svg>
    </a>
  </div>
</div>
